---
description: Neovim headless 无头模式维护与验收流程（install / Lazy / checkhealth）
alwaysApply: true
---

# Headless 无头测试与维护流程

## 标准验收（安装 → 无头脚本）

Win10/Win11 与 Unix 共用 [scripts/headless_validate.sh](scripts/headless_validate.sh)：

```bash
cd ~/.config/nvim
eval "$(fnm env --use-on-cd)"   # 无头前建议

# 1) 安装/更新（末尾默认跑无头验收；慢网络可 NVIM_SKIP_HEADLESS=1 ./install.sh）
./install.sh
# Windows 也可：install.cmd

# 2) 单独重跑无头验收
./scripts/headless_validate.sh
```

脚本内容：Lazy update（`NVIM_SKIP_LAZY_UPDATE=0` 启用，默认跳过以加速）→ 可选 `Lazy build LuaSnip` → checkhealth 落盘 → **务实 grep**。

Windows 无头前自动：`ensure_windows_user_env`（展开 USERPROFILE/XDG）、`setup_headless_proxy`（默认 7890，`USE_PROXY=0` 关闭）。

## 务实 vs 严格 grep

**务实（脚本默认）** — 失败条件：

```bash
grep -iE '^- ERROR|❌' docs/nvim_checkhealth_final.log
# 以及 packer 残留、%USERPROFILE% 路径等可修复 WARNING（见 TROUBLE_SHOOT.md）
```

**白名单 WARNING**（headless/dumb 或可选依赖，不 fail）：

- `Slow shell invocation`
- terminfo `key_backspace` / `key_dc`
- luasnip jsregexp（Windows 无 make 时）

**严格口径**（全模块零 WARNING）：

```bash
grep -iE '^- ERROR|^- WARNING|❌|⚠' docs/nvim_checkhealth_final.log || echo "CLEAN"
```

排除 treesitter 图例行 `x) errors found in the query`（说明文字，非 ERROR）。

## `--headless` 要点

| 场景 | 推荐命令片段 | 说明 |
|------|----------------|------|
| 加载完整配置 | `-u init.lua` | 必须，否则 lazy/插件/路径检测不生效 |
| 退出 | `-c "qa!"` | 强制退出，避免挂起 |
| Lazy 更新 | `-c "Lazy! update"` + `-c "sleep 90"` | Mason 未完成前勿 `qa!` |
| Lazy 同步/钉版本 | `-c "Lazy! sync"` / `-c "Lazy! sync nvim-treesitter"` | 改 `branch`/`commit` 后必须 sync |
| 健康检查落盘 | `-c "checkhealth" -c "w! path.log"` | 比 `redir` 更可靠 |
| 等待插件就绪 | `-c "lua vim.wait(ms, fn)"` | lazy=false 插件也需等待 lazy 引导 |

## 环境前置

- **uv**：`~/.config/nvim/venv/nvim-python` 与 `g:python3_host_prog`（`init.lua` 自动检测）。
- **fnm**：无头前 `eval "$(fnm env --use-on-cd)"`。
- **Neovim**：0.11.x / 0.12.x；`init.lua` 使用 `vim.fs.joinpath or vim.fs.join`；`nvim-treesitter` 保持 `branch = "master"`（0.11 API）。

## Agent 执行约束

- 插件升级或 `lazy-lock.json` 变更后，跑 `./scripts/headless_validate.sh` 或完整 `./install.sh`。
- 证据文件：`docs/nvim_checkhealth_final.log`。
- 记忆：改 `PROJECT_MEMORY.md` 后 `./install.sh` 同步 agent 配置。
